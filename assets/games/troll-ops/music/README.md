Drop the three Troll Ops radio tracks here as mp3 files, then uncomment and
fill in the entries in `../music.js` (`TRACKS` array) with matching
`title`, `artist`, and `src: "assets/games/troll-ops/music/<filename>.mp3"`.
